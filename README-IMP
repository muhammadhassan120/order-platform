# README-IMP

This old long handoff file has been intentionally reduced.

Use:

```text
CLIENT_HANDOFF_GUIDE.md
docs/runbook.md
PLACEHOLDERS.md
```

Those files match the current structure:

```text
infra/main.tf
infra/variables.tf
infra/outputs.tf
infra/modules/
```

Do not use older notes that mention deleted root Terraform files, the old singular module directory, hardcoded ECR account IDs, local SSH seed key paths, or the old state bucket.
