what to do before teraform apply and after.
BEFORE:
go to /infra/secuity.tf 
REPLACE THIS admin_cidr  = "<YOURU IP>"

2- create s3 bucket from the aws console and copy the name of the bucket.
go to infra/provider.tf , you should see this:
 backend "s3" {
    bucket = "event-driven-state-key"
    key    = "event-driven/terraform.tfstate"
    region = "us-east-2"
  }

  replace with "bucket = "event-driven-state-key"
  your actual s3 bucke name. 

  after terraform apply : 

  go to /jenkins/scripts/rollback.sh
  replace this :
  : "${ECR_REPO:=<YOUR ECR REPO URL>}"

put your actual ecr repo url 

3-  `ARTIFACTS_S3 = 'PUT_YOUR_ARTIFACTS_BUCKET_NAME_HERE'`
  - Replace with your artifacts bucket name.

  ## jenkins/Jenkinsfile.infra
- `TF_STATE_BUCKET = 'PUT_YOUR_TERRAFORM_STATE_BUCKET_HERE'`
  - Replace with your Terraform state S3 bucket.

## jenkins/scripts/rollback.sh
- `--repository-name PUT_YOUR_ECR_REPOSITORY_NAME_HERE`
  - Replace with your ECR repository name, not full URL.
- `ECR_REPO:=PUT_YOUR_ECR_REPO_URL_HERE`
  - Replace with your full ECR repo URL.

## docs/runbook.md
Replace manual values when documenting or testing:
- `<alb-dns>`
- `<cloudfront-domain>`
- `<rds-endpoint>`
- `<db>`
- `<user>`
- `<password>`


-----------------------------------

after open  instnace ip adrees with port 8080.

SETUP Jenins , and install plugins suggested .
then go to settings --> credentials --> global credntial --> select secret text -->
next --> in secret section paste **ECR REPO URL**, and in id section type THIS
"ecr-repo-url" , 

before creating jenkins job ,go to secret manager select order-platform-db and retrieve 
secret values and save each value in noteapad.

ssh into jenkins ec2 instnace and intsall postgresql , run this:
sudo dnf install -y postgresql15

then run this:
psql "host=terraform-2026041711361180110000000b.czm4e04iu1pi.us-east-2.rds.amazonaws.com port=5432 dbname=mydb user=appuser password=4LfaNXld6IBDYJS4bnCZ sslmode=require" -f scripts/seed-db.sql

replace user and pasword with the values we extracted earlier .


go to jenkins and create jenkins job.
select pipeline ---> scrolll down --> click next
scroll till the end and select "pipeline script from SCM" instead of piepline in Pipeline section.
"Paste github url" select branch main and select path "jenkins/Jenkinsfile"

click build now


visit cloudfont distribution, it will be showing ui ,

before any test , go to sns and select order-platform-order-notification.

click subscription --> select gmail protocol, and enter email,

confirmation email will be sent

